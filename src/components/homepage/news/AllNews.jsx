import React from 'react';

const AllNews = ({news}) => {
    console.log(news)
    return (
        <div>
            {/* {
                news.map((n, index)=> <p key={index}>{n.category_name}</p>)
            } */}
        </div>
    );
};

export default AllNews;